spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chenqinyang/config-repo
#          searchPaths: '{profile}'
#          searchPaths: group
          searchPaths: '{application}'
#          username:
#          password:
          skipSslValidation: true
          force-pull: true
          timeout: 5


